#include <stdio.h>

main() {
double count;
puts("Please enter a number: ");
scanf("%lf", &count); //%lf is the placeholder for double type number, maximum 15 decimal places
printf("The number is %lf \n", count);
}
